---
packages_core:
  - apt-transport-https
  - ca-certificates
  - curl
  - wget
  - gnupg
  - lsb-release
  - git
  - software-properties-common

packages_python:
  - python3
  - python3-pip
  - python3-apt
  - python3-requests
  - python3-docker

packages_ops:
  - iptables
  - jq
  - tmux
  - unzip
  - rsync

# Build dependencies (optional)
# packages_build:
#   - build-essential
#   - libssl-dev
#   - libffi-dev

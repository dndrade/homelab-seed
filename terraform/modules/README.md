# Terraform Modules

Reusable building blocks shared across providers and environments.

Modules here should be:

- small
- composable
- provider-agnostic when possible

Examples:

- SSH access helpers
- inventory export for Ansible
- shared cloud-init logic

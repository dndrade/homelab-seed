# Inventory Export Module

Responsible for exporting Terraform outputs
into Ansible-compatible inventory files.

This module is the bridge between:
Terraform (infrastructure intent)
and
Ansible (configuration enforcement).
